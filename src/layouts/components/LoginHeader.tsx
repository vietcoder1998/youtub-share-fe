import { Grid } from "@mui/material";

const LoginHeader: React.FC = () => {
  return (
    <header className="px-">
      <Grid container>
        <Grid item md={3}>

        </Grid>
        <Grid item md={8}></Grid>
      </Grid>
    </header>
  );
};

export default LoginHeader