

export enum ModelName {
    user = 'user',
    authenticate = 'authenticate',
    shareLink = 'shareLink',
    unknown = '',
    video = 'video'
}

export enum SystemLanguage {
    vi = 'vi',
    en = 'en',
}

export enum CookieVariable {
    userInfo = 'sytusi',
    userToken = 'sytutk',
    userId = 'sytuuid'
}