

export enum ModelName {
    user = 'user',
    authenticate = 'authenticate',
    shareLink = 'shareLink',
    unknown = '',
    video = 'video'
}

export enum SystemLanguage {
    vi = 'vi',
    en = 'en',
}

export enum CookieVariable {
    userInfo = 'sytusif',
    userToken = 'sytutk',
    userId = 'sytuuid'
}
